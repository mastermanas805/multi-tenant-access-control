{
  "name": "@contracts/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared cross-service contracts (pure TypeScript, no framework deps) — PDP/PIP/identity/error-envelope/expense DTOs reused across all services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "jest --passWithNoTests"
  }
}
