{
  "name": "@tests/e2e-playwright",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end suite — drives EVERY customer flow through the real Demo UI (apps/web) -> gateway -> services (DESIGN §11/§13). Global setup brings the whole compose stack up + bootstraps the demo policy; teardown drops it.",
  "type": "module",
  "scripts": {
    "test:playwright": "playwright test",
    "test:playwright:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install:browsers": "playwright install --with-deps chromium",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.16.10",
    "typescript": "^5.6.2"
  }
}
